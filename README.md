# SkillShift Monorepo Root\n\nEste diretório raiz contém o subprojeto `skillshift/` com a aplicação Next.js.\n\n## Estrutura\n- `skillshift/` código da aplicação (Next.js, mocks, API routes)\n- `ProjetoX.txt` prompt mestre e especificações do produto\n- `package.json` raiz com scripts de delegação\n\n## Comandos (executar a partir da raiz)\n```bash\nnpm install   # instala dependências apenas dentro de skillshift via workspaces\nnpm run dev   # inicia Next.js em modo desenvolvimento\nnpm run build # build de produção\nnpm run start # serve build\n```\n\n## Variáveis de Ambiente\nCopie `skillshift/.env.example` para `skillshift/.env` e preencha quando tiver as chaves reais. Por enquanto, mocks garantem funcionamento básico.\n\n## Multi-Tenant\nMocks usam `tenantId` compatível com futura migração para Firestore.\n\n## Próxima Migração\nSubstituir mock store por Firestore + Genkit (flows).\n\n## Observação\nSe executar scripts diretamente dentro de `skillshift/`, efeitos são os mesmos. O wrapper facilita comandos a partir do raiz.\n"}