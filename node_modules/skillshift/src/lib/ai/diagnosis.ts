export async function diagnoseFlow(answers: Record<string, any>) {
  // Placeholder - integrate Genkit decisionFlow + embeddings later
  return {
    habilidades: ['comunicacao', 'analise'],
    gaps: ['negociacao'],
    preferencias: { estilo: 'visual', ritmo: 'rapido' },
    raw: answers
  };
}
